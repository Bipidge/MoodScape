<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Add Internet permission if not already present (Required for Firebase) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Moodscape1"
        tools:targetApi="31">
        <activity
            android:name=".MoodEntryAdapter"
            android:exported="false" />
        <!-- Signup Activity -->
        <activity
            android:name=".authentication.Signup"
            android:exported="false" /> <!-- SignIn Activity - Now the entry point -->
        <activity
            android:name=".authentication.SignIn"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- MainActivity - Hosts fragments after login -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>